# start of backend/.env.test
# Environment
NODE_ENV="test"
PORT=5001
FRONTEND_URL="http://localhost:3000"

# Database - gunakan database khusus test
DATABASE_URL="mysql://root:Foerkoeqrb3!@localhost:3306/bani_web_test"
JWT_SECRET="test-secret-key"

# Redis - untuk testing bisa pakai mock atau memory store
REDIS_ENABLE=false  # Tambahkan ini untuk bypass Redis di testing
REDIS_HOST="localhost"
REDIS_PORT=6379
REDIS_PASSWORD="yourpassword"

# SMTP - untuk testing bisa pakai nodemailer mock
SMTP_ENABLE=false  # Tambahkan ini untuk bypass SMTP di testing
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USER="test@example.com"
SMTP_PASS="testpassword"
# end of backend/.env.test